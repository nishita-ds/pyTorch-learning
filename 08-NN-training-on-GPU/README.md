# Neural Network training on GPU (PyTorch)

This project demonstrates how to train a Neural Network on GPU using PyTorch in a Jupyter Notebook (.ipynb).
It highlights the performance benefits of GPU acceleration compared to CPU training.
