# ANN on Fashion MNIST (Pytorch + GPU optimized

This project demonstrates the implementation of an Artificial Neural Network (ANN) for image 
classification using the Fashion MNIST dataset, built with PyTorch and optimized for GPU acceleration.

The notebook provides a complete deep learning pipeline—from data loading to model training and
evaluation—while leveraging GPU computation for faster performance.
