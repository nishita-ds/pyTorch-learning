# RNN Based Q and A System using Pytorch

This notebook implements a Question Answering (QA) system using Recurrent Neural Networks (RNNs) in PyTorch. The model takes a natural language question as input and predicts the most relevant answer/class based on learned patterns.
