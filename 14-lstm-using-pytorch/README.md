# Next Word Predictor LSTM using PyTorch

This project implements a Next Word Prediction model using LSTM (Long Short-Term Memory) networks in PyTorch.
Given a sequence of words, the model predicts the most probable next word.

This notebook demonstrates the complete NLP pipeline:

>Text preprocessing
>Tokenization
>Vocabulary creation
>Sequence generation
>Model building using LSTM
>Training & evaluation
