# Transfer learning using PyTorch 

In this notebook I have used Transfer learning using pytorch on Fashion Mnist dataset which you can easily get on kaggle ,this is the dataset of 60,000 rows.
We have used GPU of google colab for better and fast performance ,using transfer learning of VGG16 model we have increased the accuracy as compared to
cnn,rnn .
