# Fashion  MNIST data Classification using ANN in PyTorch

This project implements an Artificial Neural Network (ANN) using PyTorch to classify images from the Fashion MNIST dataset.
The model learns to identify different types of clothing items such as T-shirts, shoes, bags, etc.
