# CNN using PyTorch


This project demonstrates how to build and train a Convolutional Neural Network (CNN) using
PyTorch on the Fashion MNIST dataset. The goal is to classify grayscale images of clothing
items into their respective categories.
Fashion MNIST is a popular benchmark dataset consisting of 28×28 grayscale
images belonging to 10 different fashion classes.
